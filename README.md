``` bash
cd songling1
catkin_make  #or catkin build
```

命令行窗口显示小车的线速度和角速度
``` bash
cd songling1
./speed.sh
```

OpenCV显示深度相机的RGB图像和深度图
``` bash
cd songling1
./opencv.sh
```
PCL显示激光雷达的点云数据
``` bash
cd songling1
./pcl.sh
```

all in one
``` bash
cd songling1
./task1.sh
```

用里程计（odometry）计算小车移动距离 
 
``` bash
cd songling1
./task2.sh
```
